import { RemoveDecimalPipe } from './remove-decimal.pipe';

describe('RemoveDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
