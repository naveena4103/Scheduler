import { NumberToKMPipe } from './number-to-k-m.pipe';

describe('NumberToKMPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToKMPipe();
    expect(pipe).toBeTruthy();
  });
});
